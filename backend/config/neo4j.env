# Neo4j数据库连接配置
# 复制此文件为neo4j.env并根据实际情况修改

# Neo4j连接URI 
# 如果使用Neo4j Desktop或本地安装，通常为bolt://localhost:7687
# 如果使用Neo4j Aura或其他远程服务，请使用提供的连接URI
NEO4J_URI=bolt://192.168.137.1:7687

# 用户名，默认为neo4j
NEO4J_USER=neo4j  

# 密码，请修改为您的实际密码
NEO4J_PASSWORD=xLtoQvNto1J3OTdXgG3Ic9BxPKPKL09VT9jqtCeHlyI

# 额外的Neo4j配置选项
# 是否在程序启动时自动清除过期的图谱数据（超过30天未使用的文档）
NEO4J_AUTO_CLEANUP=false

# 最大并行处理的实体关系提取任务数（影响文档处理速度和资源消耗）
NEO4J_MAX_PARALLEL_TASKS=2 